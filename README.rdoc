# FAVORITE QUOTES:

[Favorite Quotes](http://afbcolumns.herokuapp.com/) is a simple website that contains some of my favorite quotes in an unordered list. Clicking the quotes will make an ajax call which will move it to the other side of the page. This ajax call also makes a "put" request, modifying the sentence object, updating its "is_left" attribute.